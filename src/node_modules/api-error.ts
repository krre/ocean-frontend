export function message(code: number): string {
    return errors[code] || "Неизвестная ошибка сервера";
}

interface Error {
    [key: number]: string | undefined
}

const errors: Error = {
    100: "Неправильный ИД или пароль"
}

export enum Code {
    AccountBlocked = 102
}
