export class Request {
    private method: string
    private params: any
    private static counter = 0;

    constructor(method: string, params: any) {
        this.method = method;
        this.params = params;
    }

    toJson() {
        let result: any = {
            method: this.method,
            id: (Request.counter++).toString(),
        }

        if (this.params) {
            result.params = this.params;
        }

        return result;
    }

    toString() {
        return JSON.stringify(this.toJson());
    }
};
