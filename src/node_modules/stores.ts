import { writable } from 'svelte/store';

export const page = writable(1);
export const filter = writable(0);
export const category = writable(0);
export const sort = writable(0);
export const vote = writable(0);
