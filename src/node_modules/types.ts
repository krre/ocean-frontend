export enum ScreenSize {
    Phone = 600,
    Tablet = 768,
    Desktop = 992,
    LargeDesktop = 1200,
}

export interface User {
    id: number;
    code: string;
    name: string;
    create_ts: any;
}
